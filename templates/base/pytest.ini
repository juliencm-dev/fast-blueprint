[pytest]
# Exclude unnecessary directories from test collection
norecursedirs = dev-db/postgres/data

# Register custom markers (e.g., asyncio for pytest-asyncio)
markers =
    asyncio: mark test as asyncio to run with pytest-asyncio

